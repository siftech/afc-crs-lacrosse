diff --git a/pngrutil.c b/pngrutil.c
index 786abd5fb..694171f50 100644
--- a/pngrutil.c
+++ b/pngrutil.c
@@ -1425,7 +1425,7 @@ png_handle_iCCP(png_structrp png_ptr, png_inforp info_ptr, png_uint_32 length)
       /* Find the keyword; the keyword plus separator and compression method
        * bytes can be at most 41 wide characters long.
        */
-      read_length = sizeof(keyword); /* maximum */
+      read_length = max_keyword_wbytes; /* maximum */
       if (read_length > length)
          read_length = (uInt)length;
 
